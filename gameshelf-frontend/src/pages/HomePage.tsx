import { Typography } from '@mui/material';

const HomePage = () => {
  return (
    <Typography variant="h2" component="h1" sx={{ mt: 4, textAlign: 'center' }}>
      Welcome to GameShelf
    </Typography>
  );
};

export default HomePage;